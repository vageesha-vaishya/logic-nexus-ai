/**
 * Auto-generated types for the core.* Postgres schema.
 *
 * Phase 0 stub: no core.* tables exist yet. Once Phase 0.10 lands the empty
 * schema and Phase 1 lifts the first tables (audit_log, outbox, llm_*, etc.),
 * run `npm run supabase:types:gen` to populate this file.
 *
 * See docs/plans/2026-05-28-modules/core.md
 */
export type Database = {
  core: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
